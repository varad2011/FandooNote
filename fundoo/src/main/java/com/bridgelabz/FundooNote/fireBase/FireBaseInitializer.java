package com.bridgelabz.FundooNote.fireBase;


public class FireBaseInitializer {
	/*
	 * @PostConstruct public void initializer() { try { FileInputStream
	 * serviceAccount = new FileInputStream("./fireBase.json");
	 * 
	 * FirebaseOptions options = new FirebaseOptions.Builder()
	 * .setCredentials(GoogleCredentials.fromStream(serviceAccount))
	 * .setDatabaseUrl("https://fandoonote.firebaseio.com") .build();
	 * 
	 * FirebaseApp.initializeApp(options); }catch(Exception e) {
	 * 
	 * } }
	 */
}
